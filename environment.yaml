name: wellcorr
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - pip
  - pytest
  - matplotlib
  - numpy
  - pandas
  - dash
  - pip:
      - streamlit
      - plotly
      - welly
      - striplog
      - streamlit-bokeh-events